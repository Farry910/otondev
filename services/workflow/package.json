{
  "name": "@otondev/workflow",
  "version": "0.0.0",
  "private": true,
  "description": "S2 — the workflow engine: the contracts §3 state machine, compare-and-set on state_version, leases with fencing tokens, wakeups, retry/backoff, compensation, and the recovery scan.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck:tests": "tsc -p tsconfig.test.json",
    "verify:postgres": "tsx bin/verify-postgres.ts"
  },
  "dependencies": {
    "@otondev/contracts": "workspace:*",
    "@otondev/sdk": "workspace:*"
  },
  "devDependencies": {
    "@otondev/testkit": "workspace:*"
  }
}
