{
  "name": "@otondev/eval",
  "version": "0.0.0",
  "private": true,
  "description": "S19 - the evaluation and conformance harness: the gate that fails the build on a safety regression, plus the fault, adversarial, canary, benchmark and regression suites.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@otondev/contracts": "workspace:*",
    "@otondev/sdk": "workspace:*",
    "@otondev/testkit": "workspace:*"
  }
}
