packages:
  - 'packages/*'
  - 'services/*'
  - 'eval'
  - 'integration'
  - 'windows/*'

# Wave-1 sessions add their own directory; the globs above are deliberately wider than what
# exists today so a new package needs no root-config edit (root config is W0/S20-owned).
onlyBuiltDependencies:
  - esbuild
  - protobufjs
